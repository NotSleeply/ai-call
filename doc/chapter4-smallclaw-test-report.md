# 第四章 主要测试（SmallClaw）

## 4.1 主要测试项与结果

| 用例ID | 主要测试               | 测试方法                                                | 结果                                      |
| ------ | ---------------------- | ------------------------------------------------------- | ----------------------------------------- |
| T-01   | Skill 功能（可见性）   | `GET /api/skills`                                       | HTTP 200，默认/模块 Skill 可见            |
| T-02   | Skill 功能（命令联动） | `POST /api/command`（携带 `skillId`）                   | 入口与参数已支持，端到端依赖模型/脚本环境 |
| T-03   | 定时任务（查询）       | `GET /api/schedules?conversationId=1`                   | HTTP 200，默认任务可正常返回              |
| T-04   | 定时任务（参数校验）   | `POST /api/schedules`（`once` 缺失 `runAt`）            | 返回 `success=false`，可拦截非法请求      |
| T-05   | 多 Agent（入口能力）   | `POST /api/command`（`agents <任务>`）                  | 命令入口已支持，端到端依赖模型环境        |
| T-06   | 会话功能（命令闭环）   | `POST /api/command`（help）+ 会话消息回写与前端展示链路 | HTTP 200，主流程闭环可用                  |

## 4.2 修正记录

| 编号 | 问题                     | 修正                         | 复测     |
| ---- | ------------------------ | ---------------------------- | -------- |
| F-01 | `tsc: command not found` | 执行 `pnpm install` 补齐依赖 | 构建通过 |

## 4.3 技术指标（本轮实测）

| 维度       | 指标          | 实测结论                             |
| ---------- | ------------- | ------------------------------------ |
| 运行速度   | 健康接口延迟  | 0.023s                               |
| 运行速度   | help 命令延迟 | 0.032s                               |
| 运行速度   | 构建耗时      | 约 3.512s                            |
| 安全性     | 非法参数拦截  | 生效（once 缺失 runAt 被拒绝）       |
| 扩展性     | 技能机制      | 支持 prompt/module 双模式与自动触发  |
| 部署方便性 | 启动流程      | install -> build -> server，步骤清晰 |
| 可用性     | 多入口一致性  | CLI 与 Web 共用同一能力中台          |

## 4.4 结论

本项目主要测试结果表明：核心接口响应快、关键功能可用、异常输入可拦截，满足当前演示版本对速度、安全、扩展与可用性的要求。
